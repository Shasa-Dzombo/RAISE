services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: raise
      POSTGRES_PASSWORD: raise
      POSTGRES_DB: raise
    ports:
      - "5344:5432"
    volumes:
      - raise_pgdata:/var/lib/postgresql/data

volumes:
  raise_pgdata:
